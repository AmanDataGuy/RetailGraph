# Deploy image deps — only what the serving path (API + agent + UI) actually imports.
# Offline/training deps (mlflow, openai, snorkel, trl, peft, ...) are intentionally excluded.
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-dotenv==1.0.1
neo4j==5.20.0
groq==1.0.0
qdrant-client==1.17.1
fastembed==0.8.0
langgraph==0.1.6
langsmith>=0.1.75,<0.2.0
streamlit==1.35.0
requests==2.32.5
numpy==1.26.4
Pillow==10.3.0
PyYAML==6.0.1
httpx==0.27.0
tenacity==8.3.0
